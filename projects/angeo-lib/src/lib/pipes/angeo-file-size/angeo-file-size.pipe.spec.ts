import { AngeoFileSizePipe } from './angeo-file-size.pipe';

describe('AngeoFileSizePipe', () => {
  it('create an instance', () => {
    const pipe = new AngeoFileSizePipe();
    expect(pipe).toBeTruthy();
  });
});
